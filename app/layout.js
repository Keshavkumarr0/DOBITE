import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "DOBITE ",
  description: " This is a simple  To-Do List application built using React and Next.js. The app allows users to add tasks with a title and description, and displays them in a list format. It uses React state management (useState) to track user inputs and task data in real time. The UI is styled using Tailwind CSS for a clean, minimal design. ",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
